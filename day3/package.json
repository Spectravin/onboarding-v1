{
  "name": "day3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
     "dev": "http-server ./dist --port 3000 --proxy http://localhost:3000? --watch --open --cors",
    "build:esbuild": "node esbuild.config.js",
    "build": "pnpm run typecheck && pnpm run build:esbuild",
    "typecheck": "tsc --noEmit",
    "start": "http-server ./dist --port 3000 --cors --inspect", 
    "build and start": "pnpm run typecheck && pnpm run build:esbuild && http-server ./dist --port 3000 --cors --inspect"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.16.1",
  "devDependencies": {
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "esbuild": "^0.25.10",
    "eslint": "^9.35.0",
    "http-server": "^14.1.1",
    "jest": "^30.1.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
